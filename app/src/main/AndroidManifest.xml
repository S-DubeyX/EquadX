<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permissions -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-feature android:name="android.hardware.camera.any" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />


    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/ic_recycle"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_recycle"
        android:supportsRtl="true"
        android:theme="@style/Theme.EquadX.NoActionBar">

        <!-- ✅ LAUNCHER (AUTO LOGIN CHECK) -->
        <activity
            android:name=".SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Auth -->
        <activity
            android:name=".LoginActivity"
            android:exported="false" />

        <activity
            android:name=".SignupActivity"
            android:exported="false" />

        <!-- Dashboards -->
        <activity
            android:name=".StudentDashboardActivity"
            android:exported="false" />

        <activity
            android:name=".AdminDashboardActivity"
            android:exported="false"
            android:theme="@style/Theme.EquadX.NoActionBar"/>

        <!-- QR -->
        <activity
            android:name=".QrScannerActivity"
            android:exported="false" />

        <activity
            android:name=".ScanSuccessActivity"
            android:exported="false" />

        <activity
            android:name=".AdminGenerateQrActivity"
            android:exported="false" />

        <!-- Features -->
        <activity
            android:name=".LeaderboardActivity"
            android:exported="false" />

        <activity
            android:name=".ReportIssueActivity"
            android:exported="false" />

        <!-- Profile -->
        <activity
            android:name=".ProfileActivity"
            android:exported="false" />

        <activity
            android:name=".EditProfileActivity"
            android:exported="false" />

        <activity
            android:name=".ScanHistoryActivity"
            android:exported="false" />

        <activity
            android:name=".AdminScanHistoryActivity"
            android:exported="false" />

        <activity
            android:name=".AuthGateActivity"
            android:exported="true"
            android:theme="@style/Theme.EquadX.NoActionBar">
        </activity>

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyDD3HXi7AxhRjIW9m9-ug2xgrDRf3zfO-k" />

        <activity
            android:name=".MapBinsActivity"
            android:theme="@style/Theme.EquadX.NoActionBar"/>


    </application>

</manifest>
